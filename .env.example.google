# Google Home Integration Environment Variables
# Copy this to your .env file and fill in your values

# Google OAuth 2.0 Configuration (REQUIRED for Google Home integration)
# Get these from Google Cloud Console -> APIs & Services -> Credentials
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
VITE_GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Google Device Access Project ID (REQUIRED for Nest devices)
# Get this from Google Device Access Console (requires $5 one-time fee)
VITE_GOOGLE_DEVICE_ACCESS_PROJECT_ID=your_device_access_project_id_here

# Optional: Enable debug logging for Google Home integration
VITE_GOOGLE_HOME_DEBUG=true

# IMPORTANT NOTES:
# 1. App works with manual entry even without these variables
# 2. Google Home integration requires both OAuth setup AND Device Access registration
# 3. Users must have actual Nest thermostats for temperature data
# 4. For production, store client_secret securely on your backend
